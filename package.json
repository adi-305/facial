{
  "name": "face-recognition-auth",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "build": "apt-get update && apt-get install -y cmake libopenblas-dev liblapack-dev libx11-dev libpng-dev libjpeg-dev && pip install --upgrade pip && pip install wheel setuptools && pip install face_recognition && pip install -r requirements.txt && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  }
}

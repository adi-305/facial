{
  "name": "face-recognition-auth",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "build": "git clone https://github.com/davisking/dlib && cd dlib && python3 setup.py install && cd .. && pip install -r requirements.txt && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  }
}
